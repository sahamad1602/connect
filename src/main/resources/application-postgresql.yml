spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: org.postgresql.Driver

  # JPA / Hibernate
  jpa:
    hibernate.ddl-auto: update
    show-sql: true